import Client from './client';
import { escape } from './util';
declare const _default: {
    Client: typeof Client;
    literal: (params: string) => string;
    escape: typeof escape;
};
export default _default;
